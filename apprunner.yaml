version: 1.0
runtime: python3
runtime-version: 3.10
build:
  commands:
    build:
      - pip3 install --upgrade pip
      - pip3 install -r requirements.txt
run:
  command: gunicorn app:app
  network:
    port: 5000
